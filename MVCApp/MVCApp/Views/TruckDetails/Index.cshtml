@model IEnumerable<MVCApp.Models.TruckDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TruckDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TruckColor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TruckRefBrands.TruckRefBrandName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TruckDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TruckColor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TruckRefBrands.TruckRefBrandName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TruckID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TruckID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TruckID })
        </td>
    </tr>
}

</table>
